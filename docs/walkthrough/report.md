# 14 条链路走查报告（bili_split.py · 0.3.3）

> 走查方案：scenario-walkthrough v3（github.com/Simiely/workbuddy-skills）
> 走查方式：深度链 Read 原文逐层验证 + Python 实测；快速链实测确认
> 日期：2026-08-31

## 总体结论

**14 条链路全部通顺，0 个真缺陷（🔴），0 个 UX 问题，1 个已知行为观察项（🟡 已文档化）。**

| 汇总 | 数量 |
|---|---|
| 深度链（Read+实测） | 8（S-01/02/05/06/07/09/12/13）|
| 快速链（实测确认） | 6（S-03/04/08/10/11/14）|
| 🔴 真缺陷 | **0** |
| 🟡 观察项 | 1（S-13，已知行为）|

## 问题清单

| 编号 | 级别 | 问题 | 位置 | 受影响链路 | 风险分 | 影响 |
|---|---|---|---|---|---|---|
| O-01 | 🟡 观察项（非缺陷） | fetch_title 在 tv client 限流时返回空 → apply 后 ARTIST 标签缺失 | bili_split.py:472（fetch_title 失败吞掉）| S-13 | L2×I2=4 | 仅标签缺失，不影响音频/歌词；split 不中断，后续可手动补写（历史上已用 android client 补取过）|

> O-01 是**容错设计**（失败不崩溃、流程继续），且已在 0.3.2 收尾时向用户披露。走查判定：不构成缺陷，可留作后续增强项（标题失败自动换 client 重试）。

## 覆盖矩阵结论

- 每行 ≥1 格 ✓ / 每列 ≥1 格 ✓（L4 状态推进 S-05、L8 边界 S-10/S-11、L9 故障注入 S-12/S-13 齐备）
- L5 并发 / L6 离线：整列留空，理由已记录（单进程 CLI 无并发；一次性执行无重放）→ 符合方案「空格写理由」
- **无盲区**，不需要补素材

## 走查验证的良好点（程序扎实之处）

1. **两阶段数据流解耦**：split 产出 cuts.json/themes.json → apply 消费，命令间零耦合（S-05 实测：空主题跳过/缺文件跳过/正常产出三态各就各位）
2. **故障降级单点归属**：client 轮询收在 download_audio（S-12 实测结构：try/except + raise last_err）；字幕回退收在 collect_subs_timed → transcribe_segments 分支（S-07 代码确认）
3. **输入校验完备**：时间戳格式错/结束早于开始都抛错（S-08 实测）；重叠仅警告不中断（S-10 实测）；缺工具报错带环境变量提示（S-11 实测）
4. **配置优先级明确**：cookie text > 文件 > 浏览器（S-06 实测三选一正确）；代理 --proxy 覆盖默认（S-02 代码确认）
5. **容错不崩溃**：标题获取失败（S-13）、apply 文件缺失（S-09）都降级继续，不产生半成品崩溃

## 结论三问

1. **链路逻辑通顺吗？** 是。14 条链从 CLI 入口到文件产出全部走通，证据为代码行号 + 实测输出。
2. **是补丁叠补丁吗？** 否。0.3.0-0.3.3 重构后每个逻辑有单点归属：降级→download_audio、字幕决策→collect_subs_timed、标签→write_flac_tags、校验→parse_cuts，无交叉纠缠。
3. **会无法操作/出 bug 吗？** 未发现新的无法操作点。O-01 是已知容错路径（不崩、可补），非新问题。

## 建议处理（按风险分排序）

- O-01（风险 4 < 8）：**可不处理**。若要增强：fetch_title 失败时自动换 client 重试一次（android），属新功能不在本次范围。
- 走查产物（docs/walkthrough/）已入库：capabilities.md + scenarios.md + 本报告，供后续变更后回归对照。

## 迭代停止判断（步骤 6）

- 🔴 清零 ✓（0 真缺陷）
- 回归全绿 ✓（14 条链全通过）
- 探索会话：脚本化走查已覆盖已知功能全集；本轮未开 SBTM（CLI 无多角色/并发/离线面，探索价值低）——**符合停止条件，闭环收敛**
